# Production values should be set in your hosting environment.
NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
NEXT_PUBLIC_AUTHOR_ID=prod-author
NEXT_PUBLIC_GCS_BUCKET=gimpish-content
# Optional: slower polling in production
NEXT_PUBLIC_POLL_INTERVAL_MS=5000
# Optional: server-only override for SSR (non-public)
# API_BASE_URL=https://api.yourdomain.com
